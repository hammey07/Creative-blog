<!-- Contact -->
<section id="contact">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="section-heading text-uppercase">Login</h2>
                <h3 class="section-subheading text-normal" style="color:white;">Login Now to create and manage your blogs!</h3>
            </div>
        </div>

        <div class="row">
            {{#if flash includeZero=true}}
                <!-- only display the flash messages html if there are messages to display -->
                <div class=" alert alert-danger">
                    {{{flash.message}}}
                </div>
            {{/if}}
            <div class="col-lg-12">

                <form action="/login" method="post">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <input type="text" id="name" type="text" placeholder="Your Name *" class="form-control" name="username" required required="required" data-validation-required-message="Please enter your name.">
                                <p class="help-block text-danger"></p>
                            </div>


                            <div class="form-group">
                                <input type="password" class="form-control" name="password" required id="password" type="password" placeholder="Your password *" required="required" data-validation-required-message="Please enter your password.">
                                <p class="help-block text-danger"></p>
                            </div>

                        </div>

                        <div class="clearfix"></div>
                        <div class="col-lg-12 text-center">
                            <div id="success"></div>
                            <button id="sendMessageButton" class="btn btn-primary btn-xl text-uppercase" type="submit">Login</button>
                            <a href="/signup"><h3 class="section-subheading text-center" style="color:white; padding-top:20px; font-style:normal; ">Don't have an account yet? Click here to Register!</h3></a>

                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>




