var createObjectURL;

if(this.URL) {
	createObjectURL = URL.createObjectURL.bind(URL);
} else if(this.webkitURL) {
	createObjectURL = webkitURL.createObjectURL.bind(webkitURL);
} else {
	createObjectURL = function() {
		return '';
	};
}

module.exports = createObjectURL;